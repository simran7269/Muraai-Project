import { ButtonDirDirective } from './button-dir.directive';

describe('ButtonDirDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonDirDirective();
    expect(directive).toBeTruthy();
  });
});
